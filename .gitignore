node_modules/
dist/
*.tsbuildinfo

# Scratch app trees created by the compile, host and degradation tests; each
# afterEach removes them, this is just a backstop against a crash mid-test
# leaving one behind.
test/fixtures/tmp-*
*.tgz
